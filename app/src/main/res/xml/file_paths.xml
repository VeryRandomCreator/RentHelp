<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Share our app's cache directory so the camera can write the full-size image there -->
    <cache-path name="camera_photos" path="." />
</paths>
